# Установка

1. Linux / macOS (рекомендуемый способ)
```bash
curl -Ls https://astral.sh/uv/install.sh | sh
```

После установки:
```bash
uv --version
```

2. Через pip (fallback)
```bash
pip install uv
```